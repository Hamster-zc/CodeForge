# Task workspaces

`codeforge run <task.md>` creates one timestamped directory here. Each workspace
contains the copied task, durable state, and human-readable plus machine-readable
artifacts. Generated task directories are intentionally ignored by Git.

